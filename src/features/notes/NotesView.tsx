import StubView from '../../app/StubView';

export default function NotesView() {
  return (
    <StubView
      title="Notes"
      sub="Markdown notes with wikilinks and backlinks."
      storageNote="foundation_med_study_v1 → notes"
    />
  );
}
