import StubView from '../../app/StubView';

export default function PlannerView() {
  return (
    <StubView
      title="Planner"
      sub="Weekly study blocks and tasks."
      storageNote="foundation_med_study_v1 → planner"
    />
  );
}
