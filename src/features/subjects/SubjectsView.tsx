import StubView from '../../app/StubView';

export default function SubjectsView() {
  return (
    <StubView
      title="Subjects"
      sub="Your subjects and topics."
      storageNote="foundation_med_study_v1 → subjects"
    />
  );
}
