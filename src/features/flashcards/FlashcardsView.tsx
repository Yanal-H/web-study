import StubView from '../../app/StubView';

export default function FlashcardsView() {
  return (
    <StubView
      title="Flashcards"
      sub="Spaced-repetition recall (SM-2+)."
      storageNote="foundation_med_study_v1 → flashcards"
    />
  );
}
