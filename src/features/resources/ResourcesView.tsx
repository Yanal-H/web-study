import StubView from '../../app/StubView';

export default function ResourcesView() {
  return (
    <StubView
      title="Resources"
      sub="Links and reference material."
      storageNote="foundation_med_study_v1 → resources"
    />
  );
}
