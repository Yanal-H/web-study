import StubView from '../../app/StubView';

export default function StudyView() {
  return (
    <StubView
      title="Study"
      sub="Modules, reader, and recall drills."
      storageNote="foundation_med_study_v1 → study.progress"
    />
  );
}
