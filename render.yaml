services:
  - type: web
    name: study-hub
    runtime: node
    plan: free
    rootDir: study-hub
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SESSION_SECRET
        generateValue: true
      - key: COOKIE_SECURE
        value: "1"
